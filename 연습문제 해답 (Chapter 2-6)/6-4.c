
/*
#include <stdio.h>
#define SQ(x) (x) * (x)
int main()
{
	 int n, i;
	 int sum = 0;

	 printf("n의 값을 입력하세요 : ");
	 scanf("%d", &n);
	 printf(">> ", sum);

	 for(i=1; i<=n; i++)
	 {
		sum += SQ(i); 
		printf("%d²+", i);
	 }

	 printf("\b = %d\n", sum);

	fflush(stdin);
	getchar();
	return 0;
}

*/