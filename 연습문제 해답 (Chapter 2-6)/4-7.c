
/*
#include <stdio.h>

int main()
{
	int month;
	printf("월(1~12) 입력: ");
	scanf("%d", &month);
	printf(">> %d월은 %s반기입니다. \n", month, (month <= 6)? "상": "하");
	//(month <= 6)? printf(">> %d월은 상반기입니다. \n", month): printf(">> %d월은 하반기입니다. \n", month);

	fflush(stdin);
	getchar();
	return 0;
}
*/