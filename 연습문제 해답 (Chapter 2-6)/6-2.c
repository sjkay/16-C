/*
#include <stdio.h>
#define MULT(x, y) ((x) * (y))
#define MSG "æ»≥Á«œººø‰?"

int main()
{
	int a = 1, b = 2;
	printf("%s\n", MSG);
	printf("%d\n", MULT(2 + a, b));

	fflush(stdin);
	getchar();
	return 0;
}

*/