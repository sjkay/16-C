
/*
#include <stdio.h>

int main()
{
	double width, height;
	double area;

	printf("밑변, 높이 : ");
	scanf("%lf %lf", &width, &height);

	area = width * height / 2.0;

	printf("밑변=%.2lfcm, 높이=%.2lfcm인 삼각형의 면적=%.2lfcm²\n", width, height, area);

	fflush(stdin);
	getchar();
	return 0;
 }
 */